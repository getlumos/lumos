# LUMOS CLI environment configuration

# Debounce delay (ms) for file watching in `lumos generate --watch`
# Default: 100, Max: 5000
LUMOS_WATCH_DEBOUNCE=200

# Enable verbose logging for debugging
# Options: error,warn,info,debug,trace
RUST_LOG=info

# Output directory override for generated artifacts
# Example: ./generated
#LUMOS_OUTPUT_DIR=

# Preferred languages for generation (comma-separated)
# Example: rust,typescript
#LUMOS_LANGS=rust,typescript

# Temporary directory for intermediate files
#LUMOS_TEMP_DIR=

