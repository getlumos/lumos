# LUMOS Development Environment Configuration
# Copy to .env and customize as needed

# Watch mode debounce duration (milliseconds, max 5000)
# LUMOS_WATCH_DEBOUNCE=100

# Logging level (trace, debug, info, warn, error)
# RUST_LOG=info

# Error monitoring DSN (optional, for LSP server)
# SENTRY_DSN=
